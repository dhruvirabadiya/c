#include<iostream>

using namespace std;

int main(){
    int length,width,total;
    cout<<"enter the length of square:";
    cin>>length;
    cout<<"enter the width of square:";
    cin>>width;
    total=length*width;
    cout<<"area of square is:"<<total;

}